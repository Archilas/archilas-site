# Cursor-like craft brief

Waitlist stays non-persisting. No Distill / Vault. MCP is intended, not live.

## Surface

- No orbs, radial washes, blobs, frost, or glow halos.
- Three solid bands: paper, one tint compare, dark product. Teal interactive accent only. Not flat white. Not muddy orbs.
- Hero is two-column on desktop: copy left, Example record right. No secondary ghost CTA.
- Product windows: solid `#0A0A0A`. No box-shadow halo.

## Copy (locked)

- H1: RAG finds passages. Archilas keeps a record.
- Sub: Preferences, decisions, open loops — Compact. Reason. Deliver.

## Motion

- Only the Example-record Compact → Reason → Deliver tab cycle auto-plays (~4s). Pause is wired. Hover also pauses. Reduced-motion static.
- Teal `#0D9488` spring underline, 200ms.
- One compare only: Retrieval vs Record. Expandable passages. Different example than the hero.

## CTAs

- Primary Join waitlist is near-black.
- Example record is the How it works target (`#how`).
