# Waitlist persistence — Upstash Redis / Vercel KV.
# Set on Vercel Production + Preview. Do not commit real values.
#
# Either pair works:
KV_REST_API_URL=
KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
